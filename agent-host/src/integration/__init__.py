"""Integration layer for Agent Host.

Contains:
- models/: DTOs for read model projections
- repositories/: MongoDB repository implementations

Note: Import specific modules directly to avoid circular imports:
    from integration.models import ConversationDto
    from integration.repositories import MotorConversationRepository
"""
