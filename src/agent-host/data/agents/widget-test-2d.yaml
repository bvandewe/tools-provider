# Widget Test Agent (2D Canvas Mode)
# A test agent that showcases all widget types in 2D canvas mode
# Use this agent to validate that all widget components render correctly on a canvas

id: "widget-test-2d"
name: "Widget Showcase (2D Canvas)"
description: "Test all widget types in 2D canvas mode"
icon: "bi-easel"
system_prompt: |
  You are a widget testing assistant for 2D canvas layouts. Your role is to:
  1. Present widgets arranged on a 2D canvas layout
  2. Allow users to interact with multiple widgets simultaneously
  3. Collect responses when the user submits the form
  4. Provide confirmation and proceed to next canvas when ready

  This is a test flow to validate widget rendering in 2D canvas mode.
  All responses are valid - just acknowledge and continue.

model: "openai:gpt-4o"
allow_model_selection: true
tools: []
conversation_template_id: "widget-showcase-2d"
is_public: true
required_roles: []
required_scopes: []
allowed_users: null
