<nav class="navbar navbar-expand-lg sticky-top">
    <div class="container-fluid">
        <a class="navbar-brand d-flex align-items-center" href="/">
            <i class="bi bi-tools text-primary me-2"></i>
            <span class="fw-bold">MCP Tools Provider</span>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarContent">
            <!-- Admin Navigation Tabs -->
            <ul class="navbar-nav nav-pills me-auto mb-2 mb-lg-0" id="admin-nav" style="display: none;">
                <li class="nav-item">
                    <a class="nav-link active" href="#" data-page="sources">
                        <i class="bi bi-cloud-arrow-down me-1"></i>
                        Sources
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#" data-page="tools">
                        <i class="bi bi-wrench me-1"></i>
                        Tools
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#" data-page="groups">
                        <i class="bi bi-collection me-1"></i>
                        Groups
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#" data-page="labels">
                        <i class="bi bi-tags me-1"></i>
                        Labels
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#" data-page="policies">
                        <i class="bi bi-shield-check me-1"></i>
                        Policies
                    </a>
                </li>
            </ul>

            <!-- Right side: theme toggle, user info, logout -->
            <div class="d-flex align-items-center gap-3">
                <!-- Theme Toggle -->
                <button type="button" class="btn btn-link nav-link p-0" id="theme-toggle"
                    title="Toggle dark/light theme" aria-label="Toggle theme">
                    <i class="bi bi-sun-fill theme-icon-light"></i>
                    <i class="bi bi-moon-fill theme-icon-dark d-none"></i>
                </button>

                <!-- Connection Status -->
                <span id="connection-status" class="badge bg-secondary" title="SSE connection status">
                    <i class="bi bi-wifi-off"></i>
                </span>

                <!-- User Info -->
                <div class="dropdown" id="user-dropdown" style="display: none;">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                        <i class="bi bi-person-circle me-1"></i>
                        <span id="user-name"></span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><span class="dropdown-item-text small text-muted" id="user-email"></span></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li>
                            <a class="dropdown-item" href="#" id="logout-btn">
                                <i class="bi bi-box-arrow-right me-2"></i>
                                Logout
                            </a>
                        </li>
                    </ul>
                </div>

                <!-- Login Button (shown when not authenticated) -->
                <button id="login-btn" class="btn btn-primary btn-sm" style="display: none;">
                    <i class="bi bi-box-arrow-in-right me-1"></i>
                    Login
                </button>
            </div>
        </div>
    </div>
</nav>
